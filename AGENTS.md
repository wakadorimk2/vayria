# Vayria repository

## Codebase navigation

- アーキテクチャ、依存関係、影響範囲、リポジトリ全体の質問では、利用可能な `codebase-memory-mcp` を使う。
- インデックスの鮮度が重要な場合は `index_status` を確認する。
- 全体構造の確認には `get_architecture` を使う。
- インデックス結果をナビゲーション用の証拠として扱う。
- 重要な実装主張は、ソース、テスト、ビルド結果で確認する。
- `index_repository` を自動実行しない。
- 単純な1ファイル作業では、不要なCBM検索を行わない。
