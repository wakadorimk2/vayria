# Avatar placement

Place a user-owned VRM file here with this exact path:

```text
public/avatar/model.vrm
```

The VRM file is intentionally ignored by Git. Reload the browser after adding
or replacing it.
